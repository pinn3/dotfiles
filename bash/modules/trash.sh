#!/usr/bin/env bash
#
# Bash trash, configuration that i don't really want
#

alias trees='tree -L 1'
alias celar='echo "Down we gOOoooOOoooOoo"'
alias aws="docker run --rm -it -v $HOME/.aws/config:/root/.aws/config:ro -v $HOME/.aws/credentials:/root/.aws/credentials:ro amazon/aws-cli:2.0.7"
